/**
 * Unified Dashboard State Store
 * 
 * Replaces multiple local useState hooks with a centralized store for:
 * - Core metrics (GES, AI episodes, EMA, momentum)
 * - User preferences (currency, theme, sidebar)
 * - Engine state (mode, running status)
 * - Wallet state
 * - Strategy state
 */

export {};
