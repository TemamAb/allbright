[{'import\';\nimport { useState, useEffect, useCallback } from \'react\';\nimport { useWallets } from \'../context/WalletContext\';\nimport { Button } from \'../components/ui/button\';\nimport { Card, CardContent, CardHeader, CardTitle } from \'../components/ui/card\';\nimport { Badge } from \'../components/ui/badge\';\nimport { Switch } from \'../components/ui/switch\';\nimport { Input } from \'../components/ui/input\';\nimport { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from \'../components/ui/select\';\nimport { toast } from \'sonner\';\nimport { Trash2, Edit2, Save, Plus } from \'lucide-react\';\n\nexport default function WalletPage() {\n  const { wallets, addWallet, updateWallet, deleteWallet, fetchBalance, totalBalance } = useWallets();\n  const [editingId, setEditingId] = useState<number | null>(null);\n  const [withdrawAmount, setWithdrawAmount] = useState(\'\');\n  const [withdrawTo, setWithdrawTo] = useState(\'\');\n  const [withdrawals, setWithdrawals] = useState<any[]>([]);\n\n  // Validation: simple Ethereum address check\n  const validateAddress = (addr: string) => {\n    return /^0x[a-fA-F0-9]{40}$/.test(addr);\n  };\n\n  // Auto-balance fetch when address + chain become non-empty\n  useEffect(() => {\n    wallets.forEach(w => {\n      if (w.address && w.chain && editingId !== w.id) {\n        // Debounce simple: ignore if already fetching\n        fetchBalance(w.address, w.chain as any);\n      }\n    });\n  }, [wallets.map(w => w.address + w.chain).join(\',\')]);\n\n  const handleSave = async (id: number) => {\n    setEditingId(null);\n    // Could persist to backend here\n    toast.success(\'Wallet updated\');\n  };\n\n  const handleDelete = (id: number) => {\n    if (confirm(\'Delete this wallet?\')) {\n      deleteWallet(id);\n      toast.success(\'Wallet deleted\');\n    }\n  };\n\n  const handleWithdraw = async () => {\n    const amount = parseFloat(withdrawAmount);\n    if (!amount || amount <= 0) {\n      toast.error(\'Invalid amount\');\n      return;\n    }\n    if (amount > totalBalance) {\n      toast.error(\'Insufficient balance\');\n      return;\n    }\n    try {\n      const resp = await (window as any).api.withdrawProfit({\n        amount,\n        toAddress: withdrawTo || undefined,\n      });\n      if (resp.success) {\n        toast.success(\'Withdrawal initiated\');\n        setWithdrawAmount(\'\');\n        // Refresh balances? Could call fetchBalance for all active wallets\n      } else {\n        toast.error(resp.error || \'Withdrawal failed\');\n      }\n    } catch (e) {\n      toast.error(\'Withdrawal request failed\');\n    }\n  };\n\n  return (\n    <div className="space-y-6">\n      <div>\n        <h1 className="text-2xl font-bold text-foreground">Wallet Management</h1>\n        <p className="text-muted-foreground">Configure wallets for profit tracking and withdrawal</p>\n      </div>\n\n      {/* Panel 1: Managed Wallets */}\n      <Card>\n        <CardHeader>\n          <CardTitle>Managed Wallets</CardTitle>\n        </CardHeader>\n        <CardContent>\n          <div className="overflow-x-auto">\n            <table className="w-full text-sm">\n              <thead>\n                <tr className="border-b border-border">\n                  <th className="text-left p-2">#</th>\n                  <th className="text-left p-2">Wallet Address</th>\n                  <th className="text-left p-2">Private Key</th>\n                  <th className="text-left p-2">Chain</th>\n                  <th className="text-right p-2">Balance (ETH)</th>\n                  <th className="text-center p-2">Validation</th>\n                  <th className="text-center p-2">Active</th>\n                  <th className="text-right p-2">Actions</th>\n                </tr>\n              </thead>\n              <tbody>\n                {wallets.map((w, idx) => (\n                  <tr key={w.id} className="border-b border-border/50">\n                    <td className="p-2">{idx + 1}</td>\n                    <td className="p-2">\n                      {editingId === w.id ? (\n                        <Input\n                          value={w.address}\n                          onChange={e => updateWallet(w.id, { address: e.target.value })}\n                          placeholder="0x..."\n                          className="h-8"\n                        />\n                      ) : (\n                        <span className="font-mono text-xs">{w.address || \'-\'}</span>\n                      )}\n                    </td>\n                    <td className="p-2">\n                      {editingId === w.id ? (\n                        <Input\n                          type="password"\n                          value={w.privateKey}\n                          onChange={e => updateWallet(w.id, { privateKey: e.target.value })}\n                          placeholder="0x..."\n                          className="h-8"\n                        />\n                      ) : (\n                        <span className="font-mono text-xs">\n                          {w.privateKey ? \'0x\' + w.privateKey.slice(0, 6) + \'...\' : \'-\'}\n                        </span>\n                      )}\n                    </td>\n                    <td className="p-2">\n                      {editingId === w.id ? (\n                        <Select\n                          value={w.chain}\n                          onValueChange={(val) => updateWallet(w.id, { chain: val as any })}\n                        >\n                          <SelectTrigger className="h-8 w-32">\n                            <SelectValue />\n                          </SelectTrigger>\n                          <SelectContent>\n                            <SelectItem value="1">Ethereum</SelectItem>\n                            <SelectItem value="8453">Base</SelectItem>\n                            <SelectItem value="42161">Arbitrum</SelectItem>\n                            <SelectItem value="10">Optimism</SelectItem>\n                            <SelectItem value="137">Polygon</SelectItem>\n                            <SelectItem value="43114">Avalanche</SelectItem>\n                            <SelectItem value="56">BSC</SelectItem>\n                            <SelectItem value="250">Fantom</SelectItem>\n                          </SelectContent>\n                        </Select>\n                      ) : (\n                        <span className="text-xs">{\n                          {\'1\': \'Ethereum\', \'8453\': \'Base\', \'42161\': \'Arbitrum\', \'10\': \'Optimism\',\n                           \'137\': \'Polygon\', \'43114\': \'Avalanche\', \'56\': \'BSC\', \'250\': \'Fantom\'}[w.chain] || w.chain\n                        }</span>\n                      )}\n                    </td>\n                    <td className="p-2 text-right font-mono text-xs">\n                      {parseFloat(w.balance || \'0\').toFixed(4)}\n                    </td>\n                    <td className="p-2 text-center">\n                      <Badge variant={w.isValid ? \'default\' : \'destructive\'}>\n                        {w.isValid ? \'Valid\' : \'Invalid\'}\n                      </Badge>\n                    </td>\n                    <td className="p-2 text-center">\n                      <Switch\n                        checked={w.active}\n                        onCheckedChange={checked => updateWallet(w.id, { active: checked })}\n                        disabled={editingId === w.id}\n                      />\n                    </td>\n                    <td className="p-2 text-right">\n                      {editingId === w.id ? (\n                        <Button size="sm" variant="ghost" onClick={() => handleSave(w.id)}>\n                          <Save size={14} />\n                        </Button>\n                      ) : (\n                        <Button size="sm" variant="ghost" onClick={() => setEditingId(w.id)}>\n                          <Edit2 size={14} />\n                        </Button>\n                      )}\n                      <Button size="sm" variant="ghast" onClick={() => handleDelete(w.id)}>\n                        <Trash2 size={14} />\n                      </Button>\n                    </td>\n                  </tr>\n                ))}\n              </tbody>\n              <tfoot>\n                <tr className="border-t border-border bg-muted/50">\n                  <td colSpan={4} className="p-2 text-right font-bold">Total (active):</td>\n                  <td className="p-2 text-right font-bold font-mono">\n                    {totalBalance.toFixed(4)} ETH\n                  </td>\n                  <td colSpan={3}></td>\n                </tr>\n              </tfoot>\n            </table>\n          </div>\n          <div className="mt-4">\n            <Button onClick={addWallet} variant="outline" size="sm">\n              <Plus size={14} className="mr-1" /> Add Wallet\n            </Button>\n          </div>\n        </CardContent>\n      </Card>\n\n      {/* Panel 2: Profit Withdrawal */}\n      <Card>\n        <CardHeader>\n          <CardTitle>Profit Withdrawal</CardTitle>\n        </CardHeader>\n        <CardContent>\n          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">\n            <div>\n              <label className="block text-sm mb-1">Available Balance</label>\n              <div className="p-3 bg-muted rounded font-mono text-lg">{totalBalance.toFixed(4)} ETH</div>\n            </div>\n            <div>\n              <label className="block text-sm mb-1">Withdraw to Address</label>\n              <Input\n                value={withdrawTo}\n                onChange={e => setWithdrawTo(e.target.value)}\n                placeholder={import.meta.env.VITE_PROFIT_WALLET_ADDRESS || \'0x...\'}\n              />\n            </div>\n            <div>\n              <label className="block text-sm mb-1">Amount (ETH)</label>\n              <div className="flex gap-2">\n                <Input\n                  type="number"\n                  step="0.01"\n                  value={withdrawAmount}\n                  onChange={e => setWithdrawAmount(e.target.value)}\n                />\n                <Button onClick={handleWithdraw}>Withdraw Profit</Button>\n              </div>\n            </div>\n          </div>\n        </CardContent>\n      </Card>\n\n      {/* Panel 3: Withdrawal History */}\n      <Card>\n        <CardHeader>\n          <CardTitle>Withdrawal History</CardTitle>\n        </CardHeader>\n        <CardContent>\n          {withdrawals.length === 0 ? (\n            <div className="text-muted-foreground text-sm">No withdrawals yet.</div>\n          ) : (\n            <table className="w-full text-sm">\n              <thead>\n                <tr className="border-b border-border">\n                  <th className="text-left p-2">Date</th>\n                  <th className="text-right p-2">Amount</th>\n                  <th className="text-left p-2">Status</th>\n                  <th className="text-left p-2">Tx Hash</th>\n                </tr>\n              </thead>\n              <tbody>\n                {withdrawals.map((w, i) => (\n                  <tr key={i} className="border-b border-border/50">\n                    <td className="p-2">{new Date(w.date).toLocaleString()}</td>\n                    <td className="p-2 text-right font-mono">{w.amount} ETH</td>\n                    <td className="p-2"><Badge variant={w.status === \'Confirmed\' ? \'default\' : \'secondary\'}>{w.status}</Badge></td>\n                    <td className="p-2 font-mono text-xs truncate max-w-[150px]">{w.txHash}</td>\n                  </tr>\n                ))}\n              </tbody>\n            </table>\n          )}\n        </CardContent>\n      </Card>\n    </div>\n  );\n}': ''}]